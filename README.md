
  # High-Fidelity Component Library

  This is a code bundle for High-Fidelity Component Library. The original project is available at https://www.figma.com/design/SEDROT9UnOhc9eCRKKpSmk/High-Fidelity-Component-Library.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  